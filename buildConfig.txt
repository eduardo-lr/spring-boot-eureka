apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: my-build-config
spec:
  runPolicy: Serial
  source:
      git:
        ref: main
        uri: 'https://github.com/pablocandela/Hola-Mundo-Spring-Docker.git'
      type: Git
  strategy:
      dockerStrategy:
      type: Docker
  output:
    to:
      kind: ImageStreamTag
      name: 'simple-webapp-docker:latest'
  triggers:
    - type: ConfigChange